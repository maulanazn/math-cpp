#include <iostream>
#include <vector>
#include <thread>
#include <atomic>
#include <algorithm>
#include <map>

namespace cppthing 
{
    int processing();
    int thread();
    char typecaster();
    std::string namechecking();
    int run_cpp();
}
