#include <iostream>

using std::cout;
using std::endl;

namespace cppthing {
    int global();
    int local();
    int external();
    int automaticduration();
    int staticduration();
    int run_scope();
}
